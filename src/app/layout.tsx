import type { Metadata } from "next";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";
import { Toaster } from "sonner";

export const metadata: Metadata = {
  title: "GitVibe — turn any repo into an AI coding prompt",
  description:
    "Open-source, self-hostable tool that converts GitHub, GitLab, Bitbucket and Gitea repositories into optimal AI coding prompts. No tracking.",
  applicationName: "GitVibe",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>
        <ThemeProvider>
          {children}
          <Toaster position="top-center" richColors />
        </ThemeProvider>
      </body>
    </html>
  );
}
